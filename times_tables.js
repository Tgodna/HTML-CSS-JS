const timesTables = (num) => {
    for (let i = 1; i <= 9; i++) {
        console.log(`${num} x ${i} = ${num * i}`);
    }
}

timesTables(9);  // This will directly print the multiplication table for 9.
