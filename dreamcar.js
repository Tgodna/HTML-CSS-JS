const car = { 
    model: 'Volvo',
    year: 2024,
    color: 'black',
    used: true
};

console.log("I'm looking for a " + car.color + " " + car.year + " " + car.model + " that is " + (car.used ? "used" : "new") + ".");
